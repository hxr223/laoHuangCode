# laohuang Skill 设计上下文

记录 Skill 功能讨论中的术语和已确认决定；本文描述拟实现行为，不表示功能已经实现。

## Status

- Skill 产品行为已完成本轮对齐；用户授权剩余实现细节按本文约定自主处理，无需逐项追问。功能尚未实现。
- 公共文件监听服务已独立实现，见 [监听服务设计](docs/file-watcher-design.md)；Skill 已确认接入现有公共服务，不另建底层文件监听器。
- 实现设计已整理在 [docs/skill-implementation.md](docs/skill-implementation.md)，包含模块边界、代码接入、消息与监听生命周期及验收测试；该文档不表示代码已实现。
- Skill 按 DSH 的职责划分为独立领域 `packages/skill/`，包含 `skill`（注册表）、`skill-filesystem`（文件来源）、`tool-skill`（工具、显式调用和目录消息）三个私有包；取代此前归入 `packages/context/skills` 的单包方案。CLI 显式装配，文件来源复用公共监听服务。

## Language

**Skill 目录**：当前可用 Skill 的名称、描述和定位信息集合，不是磁盘文件夹，也不包含全部正文。

**Skill 正文**：某个 Skill 的完整任务指令，按需提供给模型。

**Skill 自动刷新**：Skill 模块收到文件变化事件后，使缓存失效并重新加载对应资源。取消 `/reload` 命令，不再以手动重载作为 Skill 刷新入口。

## Relationships

- 一个 **Skill 目录** 包含多个 Skill 的目录项，每项定位对应的 **Skill 正文**。
- 支持目录形式 `review/SKILL.md` 和扫描根目录下的单文件形式 `review.md`。两种形式均要求顶部 YAML 填写非空的 `name`、`description`；缺少任一项就警告并跳过，不根据文件名补名称或从正文生成描述。
- 在每个扫描根目录内递归寻找 `SKILL.md`；某目录存在 `SKILL.md` 时，将该目录作为 Skill 边界，停止向其中继续扫描其他 Skill。递归扫描来源内部，不改变项目级来源只取项目根目录下两个默认位置的约定。
- 支持通过本机文件或目录的符号链接接入外部 Skill，直接读取真实文件，不复制；不把网络 URL 当作符号链接来源。支持真实目标变化后的自动刷新，以及链接改指向、失效、删除和恢复；处理链接循环并按真实文件身份去重，来源选择仍遵循已确认的优先级。
- 符号链接监听复用现有公共监听服务。当前公共服务不自动跟随符号链接，Skill 接入需解析目标并管理链接位置与目标的监听，不能只监听链接所在目录就声称覆盖外部目标变化。
- 正文中的相对资源路径以 Skill 文件所在目录为基准；符号链接以真实 Skill 文件所在目录为基准。加载正文时提供该资源基准，`references/`、`scripts/` 等辅助文件按需读取，不随正文批量加载。
- 默认发现来源采用 DSH 的用户级和项目级布局：用户级 `~/.laohuang/skills`、`~/.agents/skills`；项目根目录下 `.laohuang/skills`、`.agents/skills`。项目级读取根目录下的这两个位置，不逐级收集祖先目录中的 Skill。
- 配置 `skillDirs` 数组追加额外本地目录，保留四个默认目录；直接读取来源文件，不复制或安装，支持展开 `~`。额外目录与默认目录共用发现、缓存、公共文件监听和正文按需读取流程。
- `skillDirs` 写在现有用户配置文件顶层，默认位置为 `~/.config/laohuang/config.json`，沿用现有配置路径覆盖机制；不在 `~/.laohuang` 下另建配置文件。`~/.laohuang/skills` 仍是默认 Skill 文件目录。
- `skillDirs` 路径采用 Pi 的配置解析规则：相对路径以实际配置文件所在目录为基准，`~` 和 `~/` 展开为用户主目录，绝对路径保持其定位含义；不随启动工作目录变化。自定义配置文件位置时，解析基准随该配置文件位置变化。
- 通过现有公共文件监听服务监听用户配置文件；运行中 `skillDirs` 变化自动生效，由 Skill 模块更新来源和监听句柄。新增目录建立监听并扫描，移除目录释放对应监听并移除来源，目录重排按新顺序重新合并优先级，随后更新命令补全和待追加的目录消息。
- 刷新由变化事件驱动，不定时反复全量扫描。启动时首次扫描；文件变化后刷新受影响来源；无变化时使用缓存，正文仍按需读取。连续事件合并、防抖处理，监听恢复或事件溢出时重新检查对应范围，防止漏掉变化。
- 异常处理采用 DSH 的区分：运行中用户配置读取、解析或 Skill 配置校验失败时，报告错误，不应用本次目录配置，保留上次有效的 Skill 来源配置；之后配置变化再次尝试加载。
- `SKILL.md` 解析失败时记录警告并跳过该文件，不保留上次有效版本作为可用 Skill。重新合并时若存在其他有效同名来源则按优先级接替，否则从可用目录移除；后续加载失败不回退旧正文。历史正文保留，修复文件后通过变化事件和重新发现恢复。
- 权限错误、磁盘故障等导致目录观测不完整时，报告错误并保留上次完整目录，不把未读到的 Skill 当作已删除，不发布误导性的空目录或删除通知。首次扫描没有有效快照时明确报告发现失败；实际工具读取失败仍返回错误，不用历史正文代替。文件可读取但格式不合法属于前述跳过规则，明确不存在属于删除规则。
- 监听事件只是变化线索，不直接判断文件是否合法。配置错误在启动读取或配置自动重载时发现；Skill 错误在首次扫描、缓存失效后的重新发现或实际加载正文时发现。格式合法不代表内容已经写完整。
- 同名 Skill 的来源优先级采用 DSH 的文件来源顺序，从高到低为：项目 `.laohuang/skills`、项目 `.agents/skills`、`skillDirs` 额外目录、用户 `~/.laohuang/skills`、用户 `~/.agents/skills`。多个额外目录中，配置数组靠前的优先。
- 同一来源目录内出现同名 Skill 时，按文件路径排序，保留靠前的一个，并提示冲突涉及的两个路径；不依赖文件系统返回的扫描顺序。
- 用户通过自然语言点名 Skill，或由模型按任务与目录描述匹配，通过 Skill 工具加载正文后执行。三包之间通过内部来源接口连接；额外目录接入不包含包安装或面向第三方的插件注册机制。
- 不加入 `disable-model-invocation` 控制；所有有效且经同名优先级选中的 Skill 默认向模型提供目录信息，允许模型按需调用工具加载正文。这不保证模型每次都自动选中匹配的 Skill。
- 不加入 `user-invocable` 控制；所有有效且经同名优先级选中的 Skill 均允许用户通过 `/skill:name` 显式调用，并进入命令补全候选。
- 提供显式命令 `/skill:name 参数`：程序按名称和已确认的来源优先级解析 Skill，直接读取正文，将正文与用户参数交给模型，不依赖模型自行选择或再次调用 Skill 工具来加载本次正文。
- 显式调用的 Skill 不存在、解析失败或读取失败时，向用户显示错误，保留原输入供修改，不发送本次模型请求。
- 输入 `/skill:` 时列出可用 Skill，支持名称补全并展示描述；候选列表随 Skill 目录自动刷新。
- 新会话首次用户任务的模型请求前，追加一条包含当前可用 Skill 名称、描述的 user 目录消息，并说明需要时调用 Skill 工具；正文不提前全部注入。当前无可用 Skill 且没有历史目录时不追加空消息。
- Skill 模块通过 **Skill 自动刷新** 更新 Skill 目录缓存；目录变化时在下一次模型请求前追加一条包含完整新版目录的 user 消息，声明替代此前目录，只能使用新版目录中的名称。
- 恢复会话采用 DSH 的目录检查方式：恢复动作本身不额外强制扫描，在下一次模型请求前查询当前目录；有效内存缓存可复用，缓存缺失或失效则重新发现。与当前上下文中仍可见的最近一份历史 Skill 目录比较名称、描述，相同不重复追加，变化则追加完整新版目录；历史目录已不可见而当前有可用 Skill 时重新追加。
- 恢复会话不额外校验历史已加载正文的版本，也不因无法确认版本而要求重读；再次调用 Skill 工具时读取当前正文。此前确认的运行期间已加载正文变化通知仍保留，不因此增加跨进程恢复时的正文校验。
- 已加载且仍可用的 Skill 正文修改时，沿用此前约定：追加简短失效通知，要求再次使用前重新调用 Skill 工具。
- 用户在明确 DSH 没有正文失效通知后，再次确认保留 laohuang 的该设计：缓存刷新保证后续工具读取当前正文，失效通知则提醒模型不要直接沿用历史正文。通知针对本次运行已加载的 Skill，在下一次模型请求前追加，不自动注入新版全文或改写历史。
- 正文通知按内容哈希比较并去重：成功加载时记录交付正文的哈希及来源，文件变化后的有效正文与之比较；原样保存不通知，针对同一次加载后的同一新版不重复通知，重新加载成功后更新比较基准。请求前合并待发通知，只针对尚未交付给模型的有效变化提示重读，不为未加载的 Skill 追加正文通知，不另外缓存旧全文。
- 整个 Skill 删除后采用 DSH 的处理方式：重新发现后若无其他同名来源接替，从可用目录移除；后续工具调用返回未知或已不可用；保留历史正文，不额外追加专门的删除失效通知。所有 Skill 均不可用时，也追加空目录更新说明当前无可用 Skill。
- 自动刷新不自动追加新版正文，也不修改历史消息；所有新版正文按需读取，避免多个 Skill 同时更新导致上下文膨胀。此决定保留此前按需读取正文的约定，替代手动 `/reload` 触发方式。
- Skill 模块负责目录提示和失效通知的会话追加行为。
- 失效通知不能程序性保证模型绝不沿用历史中的旧正文；该限制已在讨论中明确并接受。

## Example dialogue

> **开发者：** DSH 删除 Skill 后更新完整目录，后续调用返回不可用，保留历史正文，不单独通知旧正文作废。
>
> **用户：** 我们也这样，监听就调用我们的监听服务。

## Flagged ambiguities

- 本轮提出的产品决策均已解决。正文哈希去重已获确认；扫描故障处理及其他实现细节由用户授权按已有约定自主确定。
- 启动时没有有效用户配置的行为沿用现有配置加载约束。
